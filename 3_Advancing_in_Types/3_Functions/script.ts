function sum(a: number, b: number): number {
    let result: number = a + b;
    return result;
};

console.log(sum(4, 8));

//Anonimo Funtions
setTimeout(() => {
    const salary:number = 1000;
    console.log(salary);

}, 2000);
